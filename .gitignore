# ── C loader build artifacts (tagger/c) ──
/tagger/c/tagger
/tagger/c/*.o
/tagger/c/tagger.skel.h

# ── shared generated CO-RE header (from bpftool btf dump) ──
/tagger/bpf/vmlinux.h

# ── Go loader build artifacts (tagger/go) ──
# bpf2go output is regenerated by `go generate`; the binary by `go build`.
/tagger/go/tagger-go
/tagger/go/tagger_bpfel.go
/tagger/go/tagger_bpfeb.go
/tagger/go/*.o

# ── editor / OS noise ──
.DS_Store
*.swp
